name: Windows SSH Workflow

on:
  workflow_dispatch:

jobs:
  secure-ssh:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: Install and Enable OpenSSH Server
        shell: powershell
        run: |
          # Install OpenSSH Server capability
          Add-WindowsCapability -Online -Name OpenSSH.Server~~~~0.0.1.0
          
          # Start SSH service and set to Automatic start
          Start-Service sshd
          Set-Service -Name sshd -StartupType 'Automatic'
          
          # Create Firewall rule for Port 22
          if (!(Get-NetFirewallRule -Name "OpenSSH-Server-In-TCP" -ErrorAction SilentlyContinue)) {
              New-NetFirewallRule -Name 'OpenSSH-Server-In-TCP' -DisplayName 'OpenSSH Server (sshd)' -Enabled True -Direction Inbound -Protocol TCP -Action Allow -LocalPort 22
          }

      - name: Create Windows SSH User
        shell: powershell
        run: |
          # Disable Password Complexity Checks for Local Account
          secedit /export /cfg "$env:TEMP\secpol.cfg"
          (Get-Content "$env:TEMP\secpol.cfg") -replace 'PasswordComplexity = 1', 'PasswordComplexity = 0' | Set-Content "$env:TEMP\secpol.cfg"
          secedit /configure /db "$env:TEMP\secpol.sdb" /cfg "$env:TEMP\secpol.cfg" /areas SECURITYPOLICY
          
          # Fixed Password with strong compliance
          $password = "Maha@123477"
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name "winssh" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "winssh"
          
          echo "SSH_PASS=$password" >> $env:GITHUB_ENV

      - name: Install Tailscale
        shell: powershell
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Establish Tailscale Connection
        shell: powershell
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-$env:GITHUB_RUN_ID
          
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 10) {
            $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
            Start-Sleep -Seconds 5
            $retries++
          }
          if (-not $tsIP) {
            Write-Error "Tailscale IP assignment failed."
            exit 1
          }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

      - name: Intercept Shutdown and Reboot
        shell: powershell
        run: |
          $fakeRebootCmd = @"
          @echo off
          echo [+] Reboot command intercepted! Refreshing SSH Service...
          net stop sshd /y
          net start sshd
          echo [+] SSH Service refreshed. Session safe!
          "@
          
          Set-Content -Path "C:\Windows\System32\reboot.bat" -Value $fakeRebootCmd -Force
          Set-Content -Path "C:\Windows\System32\shutdown.bat" -Value $fakeRebootCmd -Force

      - name: Display SSH Connection Details
        shell: powershell
        run: |
          Write-Host "==========================================" -ForegroundColor Yellow
          Write-Host "Windows SSH Connection Details:" -ForegroundColor Green
          Write-Host "IP Address: $env:TAILSCALE_IP" -ForegroundColor White
          Write-Host "Username:   winssh" -ForegroundColor White
          Write-Host "Password:   $env:SSH_PASS" -ForegroundColor White
          Write-Host "Command:    ssh winssh@$env:TAILSCALE_IP" -ForegroundColor Cyan
          Write-Host "==========================================" -ForegroundColor Yellow

      - name: Maintain Connection
        shell: powershell
        run: |
          while ($true) {
            Write-Host "[$(Get-Date)] Windows SSH Active - Press Cancel in GitHub Actions to stop"
            Start-Sleep -Seconds 300
          }
